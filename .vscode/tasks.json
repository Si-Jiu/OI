{
  "tasks": [
    {
      "type": "shell",
      "label": "Build C++",
      "command": "g++",
      "args": ["${file}",
        "-fdiagnostics-color=always",
        "-g",
        "-Wall",
        "-std=c++23",
        "-o",
        "${workspaceFolder}\\build\\${fileBasenameNoExtension}.exe"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ],
  "version": "2.0.0"
}
