#include <bits/stdc++.h>

using namespace std;

int main()
{
    int cnt = 0;
    short in;
    do
    {
        cin >> in;
        cnt += in;
    } while (in != 0);
    cout << cnt << endl;

    return 0;
}
