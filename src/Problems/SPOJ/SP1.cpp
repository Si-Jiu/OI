#include <bits/stdc++.h>

using namespace std;

int main() {
    string a;
    while (1) {
        cin >> a;
        if (a == "42")
            break;
        cout << a << ' ';
    }

    return 0;
}
