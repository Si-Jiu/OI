#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    string ch;
    cin >> ch;
    cout << "  " << ch << endl;
    cout << ' ' << ch + ch + ch << endl;
    cout << ch + ch + ch + ch + ch << endl;

    return 0;
}
