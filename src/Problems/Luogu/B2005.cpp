#include <bits/stdc++.h>

using namespace std;

int main()
{
    string ch;
    cin >> ch;
    cout << "  " << ch << endl;
    cout << ' ' << ch + ch + ch << endl;
    cout << ch + ch + ch + ch + ch << endl;

    return 0;
}
