#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    long long m, n;
    cin >> m >> n;
    cout << ceil(1.0 * m / n) << endl;

    return 0;
}
