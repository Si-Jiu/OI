#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    short n;
    cin >> n;
    cout << 8 << endl;
    cout << 4 * 3 * (n - 2) << endl;
    cout << 6 * (n - 2) * (n - 2) << endl;

    return 0;
}
