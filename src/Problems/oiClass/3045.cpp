#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    // freopen("work.in", "r", stdin);
    // freopen("work.out", "w", stdout);

    int n, m, k;
    cin >> n >> m >> k;
    int a[n];
    for (int i = 0; i < n; i++)
        cin >> a[i];
    

    return 0;
}
