#include <bits/stdc++.h>

using namespace std;

int main() {
    int homework, test, finalTest;
    cin >> homework >> test >> finalTest;
    cout << homework * 0.2 + test * 0.3 + finalTest * 0.5 << endl;

    return 0;
}
