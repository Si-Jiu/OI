#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int homework, test, finalTest;
    cin >> homework >> test >> finalTest;
    cout << homework * 0.2 + test * 0.3 + finalTest * 0.5 << endl;

    return 0;
}
