#include <bits/stdc++.h>

using namespace std;

int main() {
    unsigned long long n;
    cin >> n;
    cout << fixed << setprecision(0) << (unsigned long long)pow(2, n) - 1
         << endl;

    return 0;
}
