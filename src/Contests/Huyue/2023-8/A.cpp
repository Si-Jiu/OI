#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    // freopen("jump.in", "r", stdin);
    // freopen("jump.out", "w", stdout);

    int a, b;
    cin >> a >> b;
    cout << ceil(b * 1.0 / a) << endl;

    return 0;
}
